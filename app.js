var locations={},socket=io(),socketUpdateCounter=new Utils.MinsCounter;socket.on("update",(function(e){locations=JSON.parse(e.update),socketUpdateCounter.update(),renderMap()})),appData.refreshInterval=1e3;const updatePlayerLocs=()=>{setTimeout(updatePlayerLocs,appData.refreshInterval),vapp.$data.nowFps=socketUpdateCounter.getPerSec()};setTimeout(updatePlayerLocs,appData.refreshInterval);